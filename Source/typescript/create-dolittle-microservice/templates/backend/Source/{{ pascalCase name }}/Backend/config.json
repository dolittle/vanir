{
    "microserviceId": "{{id}}",
    "routeSegment": "{{lowerCase name}}",
    "port": 3002,
    "dolittle": {
        "runtime": {
            "port": 50057
        }
    },
    "database": {
        "name": "{{lowerCase name}}"
    },
    "eventStore": {
        "name": "event_store_{{lowerCase name}}"
    }
}
