{
    "microserviceId": "{{id}}",
{{#if portal}}
    "isRooted": true,
    "routeSegment": "",
{{else}}
    "isRooted": false,
    "routeSegment": "{{lowerCase name}}",
{{/if}}
    "port": 3002,
    "dolittle": {
        "runtime": {
            "port": 50057
        }
    },
    "database": {
        "name": "read_models_{{lowerCase name}}"
    },
    "eventstore": {
        "name": "event_store_{{lowerCase name}}"
    }
}
