{
    "microserviceId": "{{id}}",
{{#unless hasUIPrefix}}
    "isRooted": true,
    "routeSegment": "",
{{/unless}}
{{#if hasUIPrefix}}
    "isRooted": false,
    "routeSegment": "{{lowerCase name}}",
{{/if}}
    "port": 3002,
    "dolittle": {
        "runtime": {
            "port": 50057
        }
    },
    "database": {
        "name": "read_models_{{lowerCase name}}"
    },
    "eventStore": {
        "name": "event_store_{{lowerCase name}}"
    }
}
