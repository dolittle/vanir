{
    "microserviceId": "{{id}}",
{{#if portal}}
    "isRooted": true,
    "routeSegment": "",
{{else}}
    "isRooted": false,
    "routeSegment": "{{lowerCase name}}",
{{/if}}
    "port": 3002,
    "publicPath": "./public",
    "dolittle": {
        "runtime": {
            "port": 50057
        }
    },
    "database": {
        "name": "read_models_{{lowerCase name}}"
    },
    "eventStore": {
        "name": "event_store_{{lowerCase name}}"
    }
}
