/*---------------------------------------------------------------------------------------------
 *  This file is an automatically generated Command Proxy
 *--------------------------------------------------------------------------------------------*/

import gql from 'graphql-tag';

export class {{name}} {
    {{#properties}}
    {{name}}!: {{type}};
    {{/properties}}

    toGraphQL() {
        return gql`
            mutation {
                {{#path}}
                {{value}} {
                {{/path}}
                    {{actualName}}({{parameterName}}: {
                        {{#properties}}
                        {{#if includeQuotes }}
                        {{name}}: "${ this.{{name}} }"
                        {{else}}
                        {{name}}: ${ this.{{name}} }
                        {{/if}}
                        {{/properties}}
                    })
                {{#path}}
                }
                {{/path}}
            }
        `
    }
}